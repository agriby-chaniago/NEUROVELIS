# NEUROSENSE - Raspberry Pi 5 Sensor System
# Pin this to exact versions for stable deployment
# NOTE: No RPi.bme280 needed — BMP280 driver uses smbus2 directly.
smbus2==0.4.3
grove.py==0.6.0
numpy==1.26.4
flask==3.1.0
lgpio==0.2.2.0
Pillow>=10.0.0          # required for Arducam/picamera2 JPEG encoding

# Arducam / camera dependencies (install the one matching your hardware):
#   CSI ribbon cable (recommended):
#     sudo apt install -y python3-picamera2
#   USB Arducam fallback:
#     pip install opencv-python-headless
# picamera2 is not listed here — install via apt on Raspberry Pi OS.
# OpenCV fallback for USB Arducam (or when CSI camera not detected):
opencv-python-headless>=4.9.0
mediapipe>=0.10.14

# ML inference runtime (for model_inference/artifacts/model.pkl + scaler.pkl)
# IMPORTANT: If your model was trained with a specific scikit-learn version,
# pin this to that exact version on Raspberry Pi for maximum pickle compatibility.
scikit-learn>=1.4,<2
joblib>=1.3
neurokit2>=0.2.8

# Development / testing only
pytest==8.3.4
